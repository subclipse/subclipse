# -----------------------------------------------------------------------------
# build.properties
# This file is referenced by the build.xml file.
#
# $Id$
# -----------------------------------------------------------------------------


# -----------------------------------------------------------------------------
# all jar needed
# -----------------------------------------------------------------------------
lib.dir=../lib
build.lib.dir=../build/lib

svnjavahl.jar=${lib.dir}/svnjavahl.jar
ant.jar=${lib.dir}/ant.jar
svnClientAdapter.jar=${lib.dir}/svnClientAdapter.jar
svnant.jar=${build.lib.dir}/svnant.jar


# urlRepos : the url of the test repository
# by default it is test_repos using file:/// protocol (see build.xml)
# urlRepos=file:///F:/Programmation/Projets/subversion/svnant/test/test_repos
#urlRepos=file:///home/cedric/projects/subversion/subclipse/svnant/test/test_repos

# if you want to use svn protocol, launch "svnserver -d -r test_repos" in test directory
#urlRepos=svn://localhost

javahl=true
