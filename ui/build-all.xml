<?xml version="1.0" encoding="UTF-8"?>
<project name="org.tigris.subversion.subclipse" default="build.jars" basedir=".">

	<property name="bootclasspath" value=""/>
	<property name="ws" value="win32"/>
	<property name="os" value="win32"/>
	<property name="arch" value="x86"/>
	<property name="javacFailOnError" value="true"/>
	<property name="javacDebugInfo" value="on"/>
	<property name="javacVerbose" value="true"/>

	<target name="init" depends="properties">
		<property name="plugin" value="org.tigris.subversion.subclipse"/>	
		<property name="ui-plugin" value="org.tigris.subversion.subclipse.ui"/>
		<property name="core-plugin" value="org.tigris.subversion.subclipse.core"/>		
		<property name="version.suffix" value="_0.8.1"/>
		<property name="full.name" value="${plugin}${version.suffix}"/>
		<property name="temp.folder" value="${basedir}/temp.folder"/>
		<property name="plugins.folder" value="${temp.folder}/eclipse/plugins"/>		
		<property name="core.folder" value="${basedir}/../core"/>
		<property name="ui.folder" value="${basedir}"/>		
		<property name="plugin.destination" value="${basedir}"/>
		<property name="build.result.folder" value="${basedir}"/>
	</target>

	<target name="properties" if="eclipse.running">
		<property name="build.compiler" value="org.eclipse.jdt.core.JDTCompilerAdapter"/>
	</target>

	<target name="build.jars" depends="init" description="Build all the jars for the plug-in: org.tigris.subversion.subclipse.">
		<available property="SVNPluginUI.jar" file="${build.result.folder}/SVNPluginUI.jar"/>
		<available property="SVNPluginCore.jar" file="${build.result.folder}/SVNPluginCore.jar"/>		
		<ant antfile="${core.folder}/build.xml" target="SVNPluginCore.jar">
			<property name="core.src.folder" value="${core.folder}/src"/>
			<property name="core.basedir" location="${core.folder}"/>							
		</ant>
		<ant antfile="${ui.folder}/build.xml" target="SVNPluginUI.jar"/>
	</target>

	<target name="build.sources" depends="init">
		<available property="SVNPluginUIsrc.zip" file="${build.result.folder}/SVNPluginUIsrc.zip"/>
		<available property="SVNPluginCoresrc.zip" file="${build.result.folder}/SVNPluginCoresrc.zip"/>
		<ant antfile="${ui.folder}/build.xml" target="SVNPluginUIsrc.zip"/>
		<ant antfile="${core.folder}/build.xml" target="SVNPluginCoresrc.zip">	
			<property name="core.src.folder" value="${core.folder}/src"/>	
			<property name="core.basedir" location="${core.folder}"/>				
		</ant>	
	</target>

	<target name="build.zips" depends="init">
	</target>

	<target name="clean" depends="init" description="Clean the plug-in: org.tigris.subversion.subclipse.ui of all the zips, jars and logs created.">
		<delete file="${build.result.folder}/SVNPluginCore.jar"/>
		<delete file="${build.result.folder}/SVNPluginCoresrc.zip"/>
		<delete file="${build.result.folder}/SVNPluginUI.jar"/>
		<delete file="${build.result.folder}/SVNPluginUIsrc.zip"/>
		<delete file="${plugin.destination}/${full.name}.jar"/>
		<delete file="${plugin.destination}/${full.name}.zip"/>
		<delete dir="${plugins.folder}"/>
	</target>
	
	<target name="gather.sources" depends="init" if="destination.temp.folder">
		<ant antfile="${ui.folder}/build.xml" target="gather.sources">
			<property name="destination.temp.folder" value="${plugins.folder}/"/>
		</ant>
		<ant antfile="${core.folder}/build.xml" target="gather.sources">	
			<property name="destination.temp.folder" value="${plugins.folder}/"/>
		</ant>		
	</target>	

	<target name="gather.bin.parts" depends="init" if="destination.temp.folder">
		<ant antfile="${ui.folder}/build.xml" target="gather.bin.parts">
			<property name="destination.temp.folder" value="${plugins.folder}/"/>
		</ant>
		<ant antfile="${core.folder}/build.xml" target="gather.bin.parts">	
			<property name="destination.temp.folder" value="${plugins.folder}/"/>
			<property name="core.basedir" location="${core.folder}"/>							
		</ant>
		<copy todir="${temp.folder}/eclipse">
			<fileset dir="${core.folder}" includes="libdb40.dll,libeay32.dll,ssleay32.dll"/>
		</copy>				
	</target>	

	<target name="zip.plugin" depends="init" description="Create a zip containing all the elements for the plug-in: org.tigris.subversion.subclipse.ui.">
		<delete dir="${temp.folder}"/>
		<mkdir dir="${plugins.folder}"/>
		<antcall target="build.jars"/>
		<antcall target="build.sources"/>
		<antcall target="gather.bin.parts">
			<param name="destination.temp.folder" value="${plugins.folder}/"/>
		</antcall>
		<antcall target="gather.sources">
			<param name="destination.temp.folder" value="${plugins.folder}/"/>
		</antcall>	
		<delete>
			<fileset dir="${plugins.folder}" includes="**/*.bin.log"/>
		</delete>
		<antcall target="zip.folder"/>
	</target>
	
	<target name="zip.folder" depends="init">
		<zip zipfile="${plugin.destination}/${full.name}.zip" basedir="${temp.folder}" filesonly="true" update="no" excludes="**/*.bin.log"/>
	</target>

</project>
